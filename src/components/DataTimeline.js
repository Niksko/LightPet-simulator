import React, { Component } from 'react';

var DataTimeline = React.createClass({
  render: function() {
    return (
      <div className="DataTimeline main-component">
      </div>
    );
  }
});

export default DataTimeline;
